// This component is not in use and is pending deletion.
// It was part of the text streaming feature which has been removed from the application.

const StreamedText = () => null;

export default StreamedText;
